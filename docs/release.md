# Release guide

Shopee Price Tracker v1.1.0 is the Chrome Web Store readiness release of the
complete local MVP. This guide defines the source tag and generated artifact
procedure. It does not upload, publish, deploy, or push anything.

## Release boundary

Included:

- local loopback Express backend and responsive dashboard;
- local SQLite database with ordered migrations;
- unpacked Chrome Manifest V3 extension;
- persistent manual and scheduled extension collection jobs;
- variant-aware VND history, lifecycle safeguards, and chart gaps;
- optional Telegram price-drop alerts;
- disabled-by-default local account enforcement with implemented sessions;
- a loopback-only Store extension permission boundary;
- public privacy and Chrome Web Store submission templates; and
- a reproducible Chrome Web Store upload ZIP with a SHA-256 checksum.

Excluded:

- public or LAN hosting;
- publisher-account upload, Chrome Web Store review, and publication;
- managed extension updates;
- hosted database operation and multi-device sync;
- mandatory public registration; and
- additional marketplaces.

Those exclusions require a separate security and deployment design.

## Version ownership

The v1.1.0 release version must match in:

- root `package.json`;
- `apps/server/package.json`;
- `packages/shared/package.json`;
- `apps/extension/manifest.json`; and
- the root lockfile workspace entries.

The release exporter fails if the MVP versions differ or are not numeric SemVer.

## Pre-release checklist

1. Confirm the worktree contains only the intended release changes.
2. Confirm `.env`, databases, logs, Chrome profiles, debug fixtures, and generated
   `dist` files are untracked and ignored.
3. Confirm no real Telegram token, chat ID, application password, session token,
   collection lease, cookie, or Shopee credential appears in tracked files.
4. Review `.env.example` against `apps/server/src/config/index.js`.
5. Run the complete automated gate:

   ```powershell
   npm.cmd test
   ```

6. Create the release bundle:

   ```powershell
   npm.cmd run release:prepare
   ```

7. Inspect `RELEASE-MANIFEST.json`, the unpacked extension manifest, schema
   version, migration inventory, and `CHECKSUMS.sha256`.
8. Create and inspect the Store upload package:

   ```powershell
   npm.cmd run store:prepare
   ```

9. Confirm the Store ZIP contains `manifest.json` at its root and no remote
   backend permission.
10. Load the generated release `extension` directory in Chrome and repeat the
    smallest live smoke test when release inputs changed.
11. Review the privacy policy, listing text, reviewer instructions, and current
    screenshots before upload.
12. Commit the release changes before tagging.

## Generated artifact

The ignored local artifact is:

```text
dist/releases/shopee-price-tracker-v1.1.0/
|-- .env.example
|-- README.md
|-- PRIVACY.md
|-- CHECKSUMS.sha256
|-- RELEASE-MANIFEST.json
|-- extension/
|   `-- manifest.json and the complete unpacked MV3 build
|-- database-schema/
|   |-- schema-v4.sql
|   `-- migrations/001-initial.sql through 004-variant-stock.sql
|-- store/chrome-web-store/
|   |-- listing.md
|   |-- privacy-practices.md
|   |-- reviewer-instructions.md
|   `-- assets/README.md
`-- docs/
    |-- chrome-web-store.md
    |-- setup.md
    |-- developer-guide.md
    |-- troubleshooting.md
    `-- release.md
```

The separate Chrome Web Store artifact is:

```text
dist/chrome-web-store/
|-- shopee-price-tracker-v1.1.0-chrome-web-store.zip
`-- shopee-price-tracker-v1.1.0-chrome-web-store.zip.sha256
```

The ZIP contains only the loadable extension files, with `manifest.json` at its
root. Store listing and reviewer templates remain outside the uploaded ZIP and
are retained in the general release bundle.

`schema-v4.sql` is generated by applying every migration to an empty in-memory
database and exporting the resulting tables and indexes. It contains no product,
price, user, session, or notification data. The raw migration files remain the
runtime source of truth and are copied alongside it.

`CHECKSUMS.sha256` covers every artifact file present before the checksum file
itself is written. Use it to detect accidental changes during transfer.

## Tagging

After the release commit and verification:

```powershell
git tag -a v1.1.0 -m "Shopee Price Tracker v1.1.0"
git show --stat --decorate v1.1.0
```

The tag should point to the commit that contains the synchronized versions,
release documentation and exporter.

Do not push the branch or tag unless the repository owner explicitly requests
it. If pushed later, push the reviewed branch first and the exact tag second.

## Reproducing the bundle from the tag

From a clean checkout of `v1.1.0`:

```powershell
npm.cmd ci
npm.cmd test
npm.cmd run release:prepare
npm.cmd run store:prepare
```

The file content is derived from the tagged source. `generatedAt` in the release
manifest records packaging time, so rebuilding at another time changes that
manifest and its checksum even when application inputs match.

## Installing from the bundle

The bundle is supplementary to the source tag:

- load its `extension` directory through Chrome's **Load unpacked** action;
- use `.env.example` as the private backend configuration template;
- follow `docs/setup.md`; and
- use the source tag to run the Node.js backend and migrations.

The bundle does not include `node_modules`, a private `.env`, user data, or a
live database.

## Rollback and data compatibility

Before changing application versions, stop the backend and make a private
database backup. v1.1.0 uses schema version 4.

Do not run older application code against a database after newer migrations have
changed it unless that downgrade path was explicitly tested. Source migrations
are forward-only. A release rollback therefore means restoring both the earlier
source version and its matching database backup.

## v1.1.0 verification record

Automated verification completed on August 14, 2026:

- lint and formatting checks passed;
- 49 Vitest files and 294 tests passed;
- the extension build produced 14 bundled files;
- the dashboard Chromium integration passed;
- the general release built successfully with schema version 4;
- the Store ZIP produced 14 root-relative extension files; and
- the Store ZIP SHA-256 file matched the generated archive.

Pre-submission live verification completed on August 14, 2026:

- install the unpacked v1.1.0 build and confirm local backend connectivity;
- confirm a normal Shopee capture and local snapshot upload;
- confirm queued manual price checks for two products open inactive tabs and complete;
- confirm a remote HTTPS backend URL is rejected;
- inspect the exact Store ZIP and checksum;
- capture three privacy-safe screenshots from the submitted version.

After the Store review succeeds, install the reviewed item through the selected
test distribution mode and repeat one snapshot upload and one queued manual price
check. This post-approval gate does not block creating the source tag or uploading
the reviewed package.
