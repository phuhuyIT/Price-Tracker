{
  "name": "shopee-price-tracker",
  "version": "1.1.0",
  "private": true,
  "description": "Local-first Shopee Vietnam variant price tracker.",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/server",
    "packages/shared"
  ],
  "scripts": {
    "start": "node apps/server/server.js",
    "dev": "nodemon apps/server/server.js",
    "check": "npm run lint && npm run format:check",
    "lint": "eslint apps/server apps/extension packages/shared scripts tests/unit tests/integration eslint.config.mjs vitest.config.mjs",
    "format": "prettier --write apps/server apps/extension packages/shared scripts tests/unit tests/integration *.json *.mjs",
    "format:check": "prettier --check apps/server apps/extension packages/shared scripts tests/unit tests/integration *.json *.mjs",
    "test": "npm run check && vitest run && npm run extension:build && npm run test:dashboard-browser",
    "test:watch": "vitest",
    "test:dashboard-browser": "node tests/integration/dashboard-browser.integration.mjs",
    "db:migrate": "node apps/server/src/db/migrate.js",
    "playwright:install": "playwright install chromium",
    "telegram:test": "node scripts/test-telegram.mjs",
    "extension:build": "node scripts/build-extension.mjs",
    "store:prepare": "npm run extension:build && node scripts/prepare-store-package.mjs",
    "release:prepare": "npm run extension:build && node scripts/prepare-release.mjs"
  },
  "dependencies": {
    "chart.js": "4.5.1",
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "express-rate-limit": "^8.6.1",
    "helmet": "^8.3.0",
    "pino": "^9.7.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@eslint/js": "^9.31.0",
    "esbuild": "^0.28.1",
    "eslint": "^9.31.0",
    "globals": "^16.3.0",
    "nodemon": "^3.1.10",
    "playwright": "^1.54.1",
    "prettier": "^3.6.2",
    "vitest": "^3.2.4"
  }
}
