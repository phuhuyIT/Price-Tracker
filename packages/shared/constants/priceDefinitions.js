export const SUPPORTED_CURRENCIES = Object.freeze({
  VND: 'VND',
});

export const PRICE_DEFINITIONS = Object.freeze({
  DISPLAYED_POST_VOUCHER_EXCLUDING_SHIPPING: 'displayed_post_voucher_excluding_shipping',
});

export const PRICE_TYPES = Object.freeze({
  LISTED: 'listed',
});
